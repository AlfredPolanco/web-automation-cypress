const proceedToCheckoutButton = {
	selector: '.col-sm-6 > .btn'
};

const registerLoginButton = {
	selector: '.modal-body > :nth-child(2) > a > u'
};

export const cartPageElements = {
	proceedToCheckoutButton,
	registerLoginButton,
};